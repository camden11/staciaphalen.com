import styled from "styled-components";

const A = styled.a`
  font-family: "Source Serif Pro";
  text-decoration: underline;
  color: #000;

  &:visited: {
    color: #000;
  }
`;

export default A;
